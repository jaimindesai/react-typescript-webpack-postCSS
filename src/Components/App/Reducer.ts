/* TODO : Delete This file when structure is done. Just a placeholder */

import {combineReducers} from "redux";

const rootReducer = combineReducers({
  state: (state = {}) => state
});

export {rootReducer};
